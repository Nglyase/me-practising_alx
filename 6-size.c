#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
int intsize;
float floatsize;
double doublesize;
char charsize;

{
	printf("Size of int: %zu bytes\n", sizeof(intsize));
	printf("Size of float: %zu bytes\n", sizeof(floatsize));
	return (0);
}
