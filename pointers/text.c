#include <stdio.h>

/**
 * main - entry point
 *Return: 0 on success
 */

int main(void)
{
	int num = 10;

	printf("value of num is = %d\n", num);
	printf("Address of num is = %p\n", &num);
	printf("Address of num in hexadecimal is = %p\n", &num);
	return (0);
}
